import { useRef as u, useState as n, useLayoutEffect as c } from "react";
import { createPortal as a } from "react-dom";
import d from "prop-types";
const p = ({ children: r }) => {
  const e = u(null), [t, o] = n(!1);
  return c(() => (e.current = document.createElement("div"), e.current.setAttribute("data-react-portal", "true"), document.body.appendChild(e.current), o(!0), () => {
    e.current?.remove();
  }), []), !t || !e.current ? null : a(r, e.current);
};
p.propTypes = {
  children: d.node.isRequired
};
export {
  p as Portal
};
