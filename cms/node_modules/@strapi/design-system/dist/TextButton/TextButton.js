import { jsxs as c, jsx as t } from "react/jsx-runtime";
import u from "react";
import d, { keyframes as h } from "styled-components";
import { Loader as g } from "@strapi/icons";
import { Box as p } from "../Box/Box.js";
import { Typography as x } from "../Typography/Typography.js";
import { Flex as y } from "../Flex/Flex.js";
import { buttonFocusStyle as b } from "../themes/utils.js";
const v = h`
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(359deg);
  }
`, $ = d.div`
  animation: ${v} 2s infinite linear;
  will-change: transform;
`, B = d(y)`
  background: transparent;
  border: none;

  &[aria-disabled='true'] {
    pointer-events: none;
    svg path {
      fill: ${({
  theme: r
}) => r.colors.neutral600};
    }
  }

  svg {
    display: flex;
    font-size: ${10 / 16}rem;
  }

  svg path {
    fill: ${({
  theme: r
}) => r.colors.primary600};
  }

  ${b}
`, T = u.forwardRef(({
  children: r,
  startIcon: o,
  endIcon: a,
  onClick: n,
  disabled: i = !1,
  loading: e = !1,
  ...l
}, f) => {
  const m = n && !i ? n : void 0, s = i || e;
  return c(B, {
    ref: f,
    "aria-disabled": s,
    onClick: m,
    as: "button",
    type: "button",
    ...l,
    children: [(o || e) && t(p, {
      as: "span",
      paddingRight: 2,
      "aria-hidden": !0,
      children: e ? t($, {
        children: t(g, {})
      }) : o
    }), t(x, {
      variant: "pi",
      textColor: s ? "neutral600" : "primary600",
      children: r
    }), a && t(p, {
      as: "span",
      paddingLeft: 2,
      "aria-hidden": !0,
      children: a
    })]
  });
});
T.displayName = "TextButton";
export {
  T as TextButton
};
