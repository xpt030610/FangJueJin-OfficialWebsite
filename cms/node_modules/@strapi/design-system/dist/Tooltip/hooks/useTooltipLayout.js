import { useRef as n, useLayoutEffect as c } from "react";
import { positionTooltip as s } from "../utils/positionTooltip.js";
const g = (o, e) => {
  const r = n(null), l = n(null);
  return c(() => {
    if (o) {
      const t = r.current, u = l.current, p = s(t, u, e);
      t.style.left = `${p.left}px`, t.style.top = `${p.top}px`;
    }
  }, [e, o]), { tooltipWrapperRef: r, toggleSourceRef: l };
};
export {
  g as useTooltipLayout
};
