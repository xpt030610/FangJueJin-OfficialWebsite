import e from "react";
import t from "styled-components";
import r from "prop-types";
import { Box as n } from "../Box/Box.js";
const p = t(n)`
  text-decoration: none;
  position: absolute;
  z-index: 9999;
  left: -100%;
  top: -100%;

  &:focus {
    left: ${({ theme: o }) => o.spaces[3]};
    top: ${({ theme: o }) => o.spaces[3]};
  }
`, a = ({ children: o }) => /* @__PURE__ */ e.createElement(p, {
  as: "a",
  href: "#main-content",
  background: "primary600",
  color: "neutral0",
  padding: 3,
  hasRadius: !0
}, o);
a.propTypes = {
  children: r.node.isRequired
};
export {
  a as SkipToContent
};
